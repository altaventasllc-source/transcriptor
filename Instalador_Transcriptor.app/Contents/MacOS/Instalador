#!/bin/bash
# Instalador Transcriptor - Abre Terminal y ejecuta el script de instalación

INSTALLER_DIR="$(cd "$(dirname "$0")/../Resources" && pwd)"

osascript <<EOF
tell application "Terminal"
    activate
    do script "bash '$INSTALLER_DIR/install.sh'"
end tell
EOF
